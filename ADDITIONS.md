# מכינה בית התלמוד — הוספות: יומן · מייל · דרייב

שלוש יכולות שנוספו לכרטיס התלמיד וללוח השנה. כולן מאובטחות בהרשאות זהות למערכת
(מנהל/מזכירה/מפקח = הכל; מורה = כיתותיו בלבד), ועוברות דרך **גשר Apps Script בחשבון המכינה**.

## 1) כרטיס תלמיד רחב — מייל + דרייב
כרטיס התלמיד הורחב לרוחב עם שני פאנלים:
- **מיילים עם ההורים** — חיפוש חי בג׳ימייל של המכינה לכל ההתכתבויות מ/אל כתובות ההורים
  (מתוך שדות `אימייל אב`/`אימייל אם` בכרטיס). מוצגים נושא, שולח, תאריך ותצוגת-תוכן, וכפתור "פתח בג׳ימייל".
- **קבצים בדרייב** — חיפוש חי לפי שם התלמיד; שם הקובץ + סוג + תאריך, וקישור לפתיחה בדרייב.
- **אבטחה:** לפני כל חיפוש, ה-Apps Script מאמת את ה-JWT של המשתמש מול Supabase ובודק
  שיש לו הרשאת גישה לאותו תלמיד. אם לא — נדחה. הנתונים לא נחשפים לאף אחד אחר.

## 2) סנכרון יומן דו-כיווני
לוח השנה של המערכת (`calendar_events`) ↔ יומן גוגל של המכינה:
- אירוע שנוצר/עודכן/נמחק בגוגל → מופיע/מתעדכן/נמחק במערכת.
- אירוע שנוצר/עודכן/נמחק במערכת → מסונכרן ליומן גוגל.
- **סנכרון רטרו** (כל הקיים) בהרצה הראשונה, ואוטומטי כל 15 דק' אחר כך.
- מניעת כפילויות ע"י מיפוי פנימי (PropertiesService) — בלי צורך בשינוי מסד.

## הרכיב שצריך התקנה חד-פעמית
כל השלושה מופעלים ע"י **פרויקט Apps Script אחד** שרץ בחשבון הגוגל של המכינה
(קובץ `_APPS_SCRIPT_BRIDGE.gs` — מקומי, לא ב-repo כי הוא מכיל מפתח שרת).
לאחר פריסתו כ-Web App והרצת `setup()`, שולחים את כתובת ה-Web App → מוכנסת ל-`config.js`
(`GAS_URL`) והתוצאות החיות נדלקות בכרטיס. עד אז — הכרטיס מציג כפתורי חיפוש ישירים לג׳ימייל/דרייב.

---

# תל"א — תכנית לימודים אישית (2026-08-19)

מודול חדש שמשחזר את התבנית הרשמית של המכינה (7 עמודים) ומאפשר לנהל אותה במערכת
במקום בקובץ PowerPoint לכל תלמיד.

**מסך "תל"א"** (קבוצת "מעקב ולמידה") + כפתור **תל"א** בכרטיס התלמיד + סקשן תקציר בכרטיס.

## מבנה
| לשונית | תואם לעמוד בתבנית | תוכן |
|---|---|---|
| דף הכנה | פרופיל תלמיד | רקע ומגבלות · נתונים סביבתיים · מוקדים לחיזוק · מוקדי כח + חתימת הורים |
| תכנית היעדים | התל"א | לכל תחום: קו בסיס · מטרת על · מטרה מסכמת · מטרה מבצעת · פעולות לפי בעל תפקיד · אמצעים · הישגים · הערות |
| מערכת שעות | מערכת שעות אישית | ימים א–ו × משבצות זמן; כל משבצת עם סוג התאמה מסומן בצבע |
| ישיבות צוות | תיעוד ישיבות צוות | חודש · תאריך · סיכום והמלצות · משתתפים |
| שיחות הורים | תיעוד שיחות הורים | חודש · תאריך · סיכום · הובא לידיעת |

**הדפסה / PDF** — כפתור שמייצר את כל העמודים ב-A4 לרוחב בעיצוב המכינה (נייבי+זהב, לוגו בשער),
עמוד נפרד לכל תחום. יוצא ל-PDF דרך "הדפסה → שמירה כ-PDF".

## מה ניתן לשינוי
הכל: תחומים, יעדים, שמות בעלי תפקיד (אפשר להוסיף "מטפל רגשי" וכו'), חודשים,
**שעות המערכת** (הוספה/מחיקה/שינוי של משבצות), וסוגי ההדגשה בצבע.

**תבנית כיתתית:** אפשר לשמור מערכת שעות כתבנית ("שמירה כתבנית כיתה") ואז להעתיק אותה
לתלמידים אחרים ולשנות רק את ההתאמות האישיות — במקום למלא 30 משבצות לכל תלמיד.

## מסד נתונים
`tla_plans` (תכנית + פרופיל jsonb + משבצות שעות) · `tla_goals` · `tla_meetings` · `tla_schedule` ·
`tla_class_templates`. הרצה: `supabase/migration_tla.sql` (כבר הורץ).
**הרשאות:** `can_see_student` — כל הצוות לפי הכיתות שלו, מנהל רואה הכל, אנונימי חסום לגמרי.
מחיקת תכנית מוחקת מדורג את היעדים, הישיבות והמערכת.

---

# דשבורד ודוחות — שכתוב (2026-08-19)

**הבעיה שהיתה:** הדשבורד הציג ארבע ספירות בלבד, ואחת מהן — "נוכחות היום" — היתה קשיחה
לתאריך היום. 37 רישומי נוכחות מ-16/08 פשוט לא הופיעו בשום מקום, וגם מבחנים, מעקב קריאה,
תל"א ושכר לימוד לא היו מיוצגים בכלל. הייצוא כלל רק דיווחי התנהגות.

**מה יש עכשיו:**
- **סרגל סינון:** מתאריך / עד תאריך / כיתה + קיצורי 7 ימים · 30 יום · שנה. הבחירה נשמרת בין כניסות.
- **שישה כרטיסי סיכום:** תלמידים · אחוז נוכחות בטווח · איחורים · היעדרויות · דיווחי מעקב · ממוצע מבחנים.
- **גרפים:** נוכחות לאורך זמן (קו) · התפלגות נוכח/איחור/נעדר (טבעת) · מעקב לפי קטגוריה (עמודות).
- **דוח נוכחות לפי תלמיד:** נוכח/איחור/נעדר/סה"כ + **% הגעה** ו**% בזמן** בנפרד — כי תלמיד שרק
  איחר הוצג קודם כ-100% נוכחות. ממוין מהגרוע לטוב.
- **סיכום לפי כיתה**, **מבחנים — ממוצע לפי תלמיד**, **מעקב קריאה — ממוצע לפי קטגוריה**,
  **תל"א — כמה תכניות ובאיזה סטטוס**, **שכר לימוד — שולם מול חוב**.
- **תלמידים לתשומת לב:** אחוז נוכחות נמוך, דייקנות נמוכה, או 3+ דיווחים.
- **כשאין נתונים בטווח** המסך אומר בין אילו תאריכים כן קיימות רשומות, עם כפתור "הרחב את הטווח".
- **ייצוא CSV מלא:** כל הדוחות + פירוט מלא של רישומי הנוכחות, הדיווחים והמבחנים.

הכל מכבד הרשאות: `accessibleIds()` מסנן לכיתות שהמשתמש רואה, ומקטע שכר הלימוד מוצג רק למי
שיש לו גישה למסך `tuition`.

---

# כרטיס התלמיד — השלמת הסנכרון (2026-08-19)

בדיקה של כל מקורות הנתונים מול מה שהכרטיס באמת הציג העלתה שלושה פערים:

1. **נוכחות** הוצגה כשורת ספירה מצטברת בלבד ("נוכח 1 · איחורים 0 · נעדר 0") — בלי אחוזים,
   בלי תאריכים ובלי הרישומים עצמם. זה היה המודול היחיד עם נתונים אמיתיים במערכת, ודווקא
   הוא היה הדל ביותר בכרטיס. עכשיו: **% הגעה** ו**% בזמן**, פילוח נוכח/איחור/היעדרות,
   וחמשת הרישומים האחרונים עם תאריך עברי ולועזי וסימון צבע לפי סטטוס.
2. **טפסים וחתימות** לא הופיעו בכרטיס בכלל. עכשיו: שם הטופס, סטטוס (נחתם/ממתין), שם החותם
   ותאריך החתימה — חמשת האחרונים.
3. **דיווחים קוליים** לא הופיעו בכרטיס. עכשיו: סוג הדיווח, המלמד, תמצית הטקסט ודרגת החומרה —
   ורק למשתמש שיש לו גישה למסך `voicereports` (מנהל).

מקורות שכבר היו מחוברים ונשארו: פרטי רישום מלאים, התנהגות, מבחנים, ציוני תפקוד, רפואי,
שיחות, אסיפות הורים, קריאה, כתיבה, מעקב קריאה, תל"א, שכר לימוד, משימות, מיילים ודרייב.
`calendar_events` אינה משויכת לתלמיד ולכן אין לה ייצוג בכרטיס.

---

# תיקוני איכות (2026-08-19)

**שם התלמיד הוכפל במסמכים.** רשומות התלמידים נשמרות עם `name` = שם מלא (למשל
"מיכאל יעקב גלזר") ובנוסף `family` = "גלזר" בנפרד. שישה מקומות בקוד שרשרו את שניהם
ויצרו "גלזר מיכאל יעקב גלזר" — כולל **אישור לימודים רשמי** שמודפס להורים ולמוסדות.
נוצר מקור אמת אחד — `UI.fullName(student)` ב-`js/ui.js` — וכל המקומות עברו אליו:
`certificate.js` (גוף האישור + טופס ההנפקה), `reading-assess.js` (כותרת ההערכה + טבלת
הכיתה), `tla.js`, `dashboard.js`.

**"הדפסה" בכרטיס התלמיד הדפיסה את כל הדף** שמאחורי המודאל. עכשיו נוסף `printing-card`
ל-`body` בזמן ההדפסה, וכלל `@media print` מסתיר את כל שאר הדף ומותיר את הכרטיס בלבד,
בלי כפתורי הפעולה. ה-class מוסר ב-`afterprint` (ועם timeout ביטחון לדפדפנים שלא יורים אותו).

---

# פאנל דיוור להורים + סורק מיילים אוטומטי (2026-09-04)

## תוספת 1 — מסך "שליחת דיוור" (`js/messaging.js`, adminOnly)
מסך חדש שמאפשר למזכירה/מנהל לשלוח דיוור להורים ב-4 ערוצים:

| ערוץ | תיאור |
|---|---|
| **מייל בלבד** | שולח מייל HTML מחשבון המכינה 6787012@gmail.com לכל אבא + אמא. |
| **הודעה קולית בלבד** | מעלה קובץ שמע לשלוחה בקו 0733518751 + צינתוק חינמי לנרשמים. |
| **מייל + הודעה קולית** | שילוב של שניהם. |
| **מייל + צינתוק לנרשמים בלבד** | מייל לכולם + צינתוק חינמי בלי חיוב יחידות. |

**תבניות מוכנות (8 זרעיות):** זימון לאסיפת הורים · דיווח אירוע · ברכת חג · חופש · הודעה דחופה · תודה · הודעת בוחן · תזכורת תשלום. ניתן להוסיף/לערוך תבניות משלכם (טבלה `message_templates`). Placeholders: `{{student_name}}`, `{{class_name}}`, `{{parent_name}}` — מוחלפים אוטומטית לפי הנמען.

**בחירת נמענים:** כל התלמידים / לפי כיתה / בחירה ידנית (עם חיפוש). מוצג לפני שליחה כמה כתובות מייל ייכללו.

**תוכן ההודעה הקולית:** TTS דרך Gemini (`geminiSpeak` הקיים ב-yemot.js) · הקלטה מהדפדפן · קובץ מוכן.

**היסטוריה:** כל שליחה נכתבת ל-`message_log` (מנהל/מזכירה בלבד). כפתור "היסטוריה" מציג את 200 האחרונות.

**שיפור AI:** כפתור "שיפור בעזרת AI" מוסר את הטקסט של הנושא/הגוף ל-Gemini לניסוח מקצועי — שומר על placeholders.

**קבצים:** `js/messaging.js` (מודול חדש) · `supabase/migration_messaging.sql` (טבלאות + זרעים + RLS) · הרחבת `_APPS_SCRIPT_BRIDGE.gs` (`doPost` + `sendBulkMail` + endpoint `action=sendMail`) · רישום ב-`app.js` + `index.html` + `sw.js` (v93).

## תוספת 2 — סורק מיילים אוטומטי → דיווחי AI בכרטיס
כל שעתיים (טריגר Apps Script חדש) — `scanEmailsForStudentReports` סורק מיילים חדשים ב-INBOX של חשבון המכינה, מזהה מיילים שקשורים לתלמיד קונקרטי, שולח את התוכן ל-Gemini לניתוח, ושומר סיכום ב-`email_reports`.

**זיהוי תלמיד** — 3 שלבים בסדר: (א) כתובת השולח מתאימה לאימייל אב/אם של תלמיד יחיד. (ב) שם המשפחה של תלמיד יחיד מופיע בנושא/גוף. (ג) שם התלמיד המלא מופיע בגוף. כל מייל שלא זוהה מסומן ב-Gmail label `AI-Scanned` (כדי לא לחזור עליו).

**⚠️ פרטיות:** רק מיילים שבהם *זוהה* תלמיד נשלחים ל-Gemini. גם עליהם — רק נושא + חתימת שולח + 1000 תווים ראשונים של הגוף. אף מייל לא זוהה = אף חלק ממנו לא עולה ל-AI.

**ניתוח Gemini** — מחזיר JSON: `summary` (2-3 שורות), `sentiment` (חיובי/ניטרלי/שלילי), `category` (הודעה מהורה / עדכון מצוות / אירוע חריג / שגרה). מייל ספאם/שיווקי מחזיר `skip:true` ולא נשמר.

**הצגה בכרטיס תלמיד** — סקשן חדש "דיווחי AI ממיילים" (מתחת לסקשן המיילים הרגיל), מציג את 30 הרשומות האחרונות: נושא, סנטימנט (צבע), קטגוריה, סיכום, שולח, תאריך, וכפתור פתיחה בג'ימייל.

**RLS** — `email_reports` פתוחה לקריאה לפי `can_see_student` (=אותה מדיניות של כל דבר תלמידי: מנהל/מזכירה/מפקח רואים הכל, מלמד רואה כיתותיו). כתיבה: רק service_role (Apps Script). מחיקה: רק מנהל.

**קבצים:** `supabase/migration_email_reports.sql` · הרחבת `_APPS_SCRIPT_BRIDGE.gs` (`scanEmailsForStudentReports` + `geminiAnalyze` + טריגר כל 120 דק' ב-`setup`) · הצגה ב-`js/students.js`.

## מה נשאר ידנית ליוסף אחרי push
1. **הרצת שתי המיגרציות** ב-Supabase SQL Editor: `migration_messaging.sql`, `migration_email_reports.sql`.
2. **פריסת הגשר החדש** — Apps Script deploy חדש של `_APPS_SCRIPT_BRIDGE.gs`. לעדכן את URL ה-Web App החדש ב-`_CREDENTIALS.md` וב-`js/config.js` (`GAS_URL`).
3. **הרצת `setup()` פעם אחת** דרך `?action=setup&key=bht-setup-9f3a2c` — מרשם את הטריגר החדש של סריקת המיילים.
