# מבנה הניתוב של קו מכינת בית התלמוד (ימות המשיח)

DID (מספר הקו): `0733518751` | תיקיית שורש: `ivr2:/`

## שלב 1 — מה קורה מרגע החיוג

1. **פתיח** — מושמע `M0000.wav` ("שלום, הגעתם לקו מכינת בית התלמוד") — קובץ שמע נפרד, מושמע תמיד ראשון, לפני כל תפריט. נוצר עם Edge TTS (קול גברי `he-IL-AvriNeural`, חינמי).
2. השיחה נכנסת לקובץ `ivr2:/ext.ini` (השורש). הוא לא תפריט רגיל — יש בו:
   ```
   type=menu
   go_to_folder=/0
   go_to_folder_from_file=yes
   ```
   `go_to_folder_from_file=yes` אומר: **לפני** שהולכים ליעד הקבוע (`/0`), בדוק את מספר המתקשר מול קובץ `FileGoToFolder.ini` — אם המספר שלו רשום שם, לך ליעד שרשום לידו במקום.

## שלב 2 — טבלת הניתוב לפי מספר (`FileGoToFolder.ini`)

קובץ אחד גדול, שורה לכל מספר טלפון:
```
<מספר>=<יעד>
```
שלושה סוגי רשומות בקובץ הזה כרגע:

| סוג | דוגמה | יעד |
|---|---|---|
| **הורים** (נבנה אוטומטית מ-Supabase, סקריפט `build_routing.py`) | `0527106863=3` | ישר לשלוחת השיעור של הילד (1/2/3/4) |
| **צוות** (12 מספרים מתוך `WhiteList.ini`) | `0556742853=staff` | שלוחה ייעודית `/staff` |
| **לא ברשימה בכלל** | — | אין רשומה → נופל ליעד הקבוע `/0` (התפריט הראשי הרגיל) |

## שלב 3 — שלוש הזרועות האפשריות

### א. הורה מזוהה → ישר לשלוחת השיעור (`/1`, `/2`, `/3`, `/4`)
תפריט menu של השיעור עצמו (1=הודעות, 2=רישום לצינתוק, 3=השארת הודעה, 9=עדכונים כלליים).
שיעור א = שלוחה 1, שיעור ב = שלוחה 2, **שיעור ג1 = שלוחה 3 (הרב יודלוב), שיעור ג2 = שלוחה 4 (הרב קרשנר)**.

### ב. מספר לא מזוהה → `/0` — התפריט הראשי הרגיל
תפריט menu עם כל 9 האפשרויות (1=שיעור א, 2=שיעור ב, 3=שיעור ג1 [הרב יודלוב], 4=שיעור ג2 [הרב קרשנר], 5=הקלטת הודעה למנהל, 6=עדכונים כלליים, 7=תפריט הקלטות מנהל, 8=הקלטות מנהל [מוגן בסיסמה 2853], 9=הורה משאיר הודעה).

### ג. איש צוות מזוהה → `/staff` — תפריט צוות ייעודי
```
type=menu
menu_voice="שלום, הגעתם לתפריט הצוות... לשמיעת ההודעות הכלליות הקישו 6.
            לדיווח קולי הקישו 9. להקלטות מנהל הקישו 8. לתפריט הראשי הקישו 0."
```
כל מקש בתפריט הזה הוא בעצם **קיצור דרך** לשלוחה המקבילה בשורש, בלי לעבור דרך `/0`:
- `6` → `go_to_folder=../6` (עדכונים כלליים — **חדש**, התווסף עכשיו)
- `9` → `go_to_folder=../9` (הקלטת הודעה)
- `8` → `go_to_folder=../8` (הקלטות מנהל — עדיין מבקש את הסיסמה 2853, בכוונה)
- `0` → `go_to_folder=/0` (בריחה לתפריט הראשי המלא אם צריך משהו אחר)

### ד. תפריט "הודעות מנהל" (`/8`, מוגן בסיסמה 2853) — הקלטה ושידור
נגיש גם מהתפריט הראשי (מקש 8) וגם מתפריט הצוות (מקש 8). אחרי הסיסמה:
- `1`-`4` → הקלטה לשיעור ספציפי | `5` → לכל השיעורים | `6` → לצוות
- `7` → **הקלטה + בחירת יעדים מתקדמת**: אחרי ההקלטה נשאלים 2 דברים ברצף —
  1. **איך לשלוח**: 1=שיחה עם הקראה מלאה (בתשלום) · 2=רק לשמור ברצף ההשמעה, בלי לשדר בכלל (חינם)
  2. **למי**: הקש 1 ואז מספרי היעדים (1=שיעור א...5=צוות, אפשר כמה יחד) — או הקש 2 ואז מספר טלפון ספציפי
  ולבסוף סולמית. כל השילובים האלה מטופלים ע"י Worker עצמאי (`beit-hatalmud-broadcast-worker`, לא קובץ בקוד הקו) שמזהה את ההקלטה האחרונה, שולף רשימת טלפונים אמיתית מה-DB, ומבצע את השידור.
  ⚠️ **"צינתוק בלבד" (זול, בלי הקראה) לרשימה שאני בוחר — לא נתמך בפועל ע"י ימות.** צינתוק אמיתי (`RunTzintuk`) עובד רק על רשימת-נרשמים-עצמאית (מקש 7 בתפריט הראשי, הורים שנרשמו בעצמם), לא על רשימת טלפונים שאני שולט בה. אין endpoint API חלופי.

## שתי בעיות "מבקש סיסמה" / "לא ממשיך" שנפתרו

**1. "מבקש סיסמה בכניסה"** — לא הייתה קשורה לאף קובץ ב-`ivr2`. הוכח דרך יומן `LogFolderEnterExit` (שיחות עברו מ-`main` ליעד ב-0 שניות, בלי עצירה) שזו הגדרה כללית בפאנל הניהול הראשי של ימות, לא נגישה דרך ה-API — נמצאה ותוקנה ישירות שם.

**2. "רק שומע פתיח, לא ממשיך"** — **M1000 הגלובלי הסטנדרטי של ימות** ("אנא הקש את מספר השלוחה") מושמע כי ה-`go_to_folder_from_file=yes` בשורש הוא לא דילוג-מיידי אלא **fallback שקורה רק אחרי timeout** (ברירת מחדל: 7 שניות × 5 ניסיונות — עד 35 שניות!). תוקן ע"י `timeout=1`+`attempts=1` בשורש, כדי שהניתוב האוטומטי יקרה כמעט מיד.
