אתה ממשיך אוטונומית בניית פרויקט תוכנה. עבוד לבד, בלי לשאול שום שאלה, בלי לחכות לאישור.

1. קרא את C:\projects\cheder-v3\_BUILD_STATE.md — הוא מכיל את המצב, חוקי הברזל, ואיזה חלק הבא לבנות.
2. קרא את C:\projects\cheder-maale-amos\BUILD_GUIDE_8_PARTS.md (הפרומפט המפורט לכל חלק).
3. בצע **חלק אחד בלבד** — הראשון שעדיין מסומן [ ] ב-_BUILD_STATE.md — במלואו:
   - בנה את הקוד: frontend (מסכים אמיתיים במקום עמודי ממלא-המקום ב-app.js/מודולים) + שאילתות Supabase דרך js/api.js (db.list/insert/update/remove). תמיד לתמוך במצב DEMO.
   - **בדיקה ויזואלית בכרום** (חובה): הרץ `python -m http.server 8090` בתיקיית cheder-v3 ברקע, ואז הרץ Playwright headless מתוך C:\Users\יוסף שניידר\maale-amos-site (יש שם playwright ב-node_modules) שטוען את המסכים שבנית, מצלם לתיקיית scratchpad, ומוודא: רינדור נקי, RTL תקין, קונסולה בלי שגיאות, אין גלישה אופקית במובייל. תקן עד שנקי. עצור את השרת בסוף.
4. חוקי ברזל: אל תיגע ב-cheder-bht-temp / cheder-maale-amos-local. אל תדחוף ל-git ואל תפרוס. אין סודות בקוד. בדוק CPU לפני תהליכים כבדים (אם >80% המתן).
5. עדכן את _BUILD_STATE.md: סמן את החלק שסיימת כ-[x] ✅ עם תאריך ותיאור קצר מה נבנה.
6. אם סיימת את חלק 8 — צור קובץ ריק C:\projects\cheder-v3\_BUILD_DONE.
7. עצור אחרי חלק אחד. סכם בקצרה בעברית מה נבנה ומה מצב הבדיקה הויזואלית.
