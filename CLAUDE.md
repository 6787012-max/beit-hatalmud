# CLAUDE.md — beit-hatalmud

## מסך הלובי + פאנל ניהול (2026-08-24)
מחליף את `מסך לובי וידאו.html` שישב בשולחן העבודה עם סדר יום קשיח בקוד (הקובץ הישן הושאר במקומו).
- **`lobby/screen.html`** = התצוגה, **`lobby/lobby_server.py`** = שרת מקומי (פורט 8484) שמגיש אותה
  ואת הסרטונים, **`js/lobby.js`** = הפאנל באתר (מודול `lobby`, adminOnly). ראה `lobby/README.md`.
- **`lobby_config`** — טבלה אחת, מפתח→jsonb: `schedule` / `menu` / `messages` / `display` / `runtime`.
  ⚠️ **קריאה מותרת ל-anon** כי המסך בקיר אינו מזוהה → **אין לשמור שם מידע אישי**.
  שורת `runtime` היא היחידה שגם anon רשאי לעדכן (רשימת תיקיות + חותמת זמן, נבדק חי ש-`display`
  ו-`insert` נחסמים) — כך הפאנל יודע אילו תיקיות קיימות במחשב הלובי ומתי הוא היה חי.
- **זרימת הסרטונים:** הפאנל שומר `videoRoot`/`videoFolders` בענן → המסך מושך כל דקה →
  אם השרת המקומי מחזיק ערכים אחרים הוא מיישר אותו (`setRoot`/`setSelection`). אין הגדרה כפולה.
- **בלי בחירת תת־תיקיות אין ניגון בכלל** (מכוון: בשורש `Z:\סרטונים` יש 4249 קבצים, כולל
  `לא_מתאים` וחומר פרטי). בזמן משבצת מסוג `lesson` המסך עובר לשעון — "בלי סרטונים באמצע".
- ⚠️ **`style.display=''` לא מבטל `display:none` שנקבע ב-CSS** — `#menuCard` נשאר מוסתר עד
  שהוחלף ל-`'block'`. אותה מלכודת קיימת בכל מקום שמסתירים ב-CSS ומראים ב-JS.
- מ-https אי אפשר לפנות ל-`http://localhost` (Mixed Content): `lobby/screen.html` שנפתח מהאתר
  עובד כלוח מידע בלבד, וזו גם ה"תצוגה מקדימה" שבפאנל. הקישור עם הסרטונים הוא localhost:8484.
- הזרעה ראשונית (סדר יום תשפ"ו + תפריט אלול מה-HTML שבהורדות): `.local/seed_lobby.py`.


## תיק מסמכים לתלמיד (2026-08-20)
- **טבלה `student_docs` + bucket פרטי `student-docs`** (`supabase/migration_student_docs.sql`, הורץ חי).
  שורה אחת מכילה או קובץ שהועלה (`source='upload'`, `path` ב-bucket) או קישור לדרייב (`source='drive'`).
- **מוסכמת נתיב חובה:** `{student_id}/{אקראי}-{שם}` — ה-policy על `storage.objects` שולף את
  `(storage.foldername(name))[1]` כמזהה תלמיד. נתיב אחר = הקובץ לא נגיש לאיש.
- **RLS אומת חי:** מחנך בלי שיוך כיתה רואה **0** מסמכים; אחרי שיוך לשיעור ב׳ ראה 21 (רק כיתתו). anon = ריק.
- **הורדה תמיד כ-blob דרך ה-API** (`storage.download`) — קישור חתום שנפתח בטאב חדש נחסם ע"י נטפרי.
- **מחיקה:** קודם שורת המטא-דאטה (עם `.select()` לאימות שה-RLS לא חסם בשקט) ורק אז הקובץ.
- **דרייב:** `.local/_drive_match.json` = מיפוי 31 תלמידים ל-46 תיקיות ב-
  `תיקי תלמידים - בית התלמוד (פרטי)` (1IRL5jc86XGxRDLpZQbgoY9uBjRq-Majz, בבעלות 6742853).
  ⚠️ בדרייב יש **שתי מוסכמות שמות** — 26/04 "משפחה פרטי" (2-3 קבצים) ו-27/04 "פרטי משפחה" (10-14 קבצים),
  ולכן לרוב התלמידים יש שתי תיקיות. הכפתור מציג את שתיהן.
  ניהול מלא של הדרייב מתוך האתר דורש OAuth של 6742853 בהיקף `drive` (הטוקן של clasp הוא 6787012/`drive.file` — לא מספיק).

## עדכון 2026-08-20 — הקבצים עברו לגוגל דרייב (לא ל-Supabase Storage)
בקשת יוסף: העלאה/צפייה/הורדה/מחיקה יתבצעו **בתוך תיקיית התלמיד בדרייב**.
- **Edge Function `drive`** (`supabase/functions/drive/index.ts`, פרוסה דרך Management API):
  list / upload / download / delete / mkdir. הדפדפן לא נוגע בגוגל — רק ב-Supabase.
- **אימות דו-שכבתי:** הפונקציה שואלת את `student_docs` **עם ה-JWT של המשתמש** (RLS=can_read_student)
  כדי לדעת אילו תיקיות מותרות לו, וכל פעולה מוגבלת לתיקיות האלה (וגם `parents` של קובץ בודד נבדק).
- **סודות** ב-Supabase secrets: `GOOGLE_CLIENT_ID/SECRET/REFRESH_TOKEN` (הטוקן של clasp, חשבון **6787012**,
  שיש לו הרשאת **writer** על `תיקי תלמידים - בית התלמוד (פרטי)` — נבדק חי).
- ⚠️ **ההיקף הוא `drive.file`:** אפשר *לראות* את כל הקבצים בתיקייה, אבל **להוריד/למחוק רק את מה
  שהמערכת עצמה העלתה**. לקבצים הישנים מוצג "פתח בדרייב" והפונקציה מחזירה הודעה מפורשת (403).
- ⚠️⚠️ **נטפרי חוסם גוף תגובה בינארי** — הורדת PDF החזירה `HTTP 418 Error in NetFree`, והדפדפן הציג
  את זה כשגיאת CORS מטעה. הפתרון: `action=download` מחזיר **base64 בתוך JSON**, והלקוח מרכיב Blob.
  **אל תחזיר את הקובץ כבינארי — זה ייכשל רק אצל משתמשי נטפרי, כלומר אצל כולם.**
- **סיווג לפי שם הקובץ** (`classify`) נותן את חיווי ה-✓/✗ לארבע קטגוריות האפיון גם למאות הקבצים
  הישנים, בלי הזנה ידנית. בהעלאה, אם השם לא מסגיר את הקטגוריה — היא מוקדמת לשם.
- ה-bucket `student-docs` שנוצר קודם **אינו בשימוש יותר** (נשאר ריק); `student_docs` משמשת עכשיו
  רק לרישום תיקיות הדרייב = מקור ההרשאה.

### הורדת הקבצים הישנים (2026-08-20, המשך)
`drive.file` לא מאפשר לגעת בקבצים שהמערכת לא העלתה, ולכן הורדתם עוברת **fallback לגשר ה-Apps Script**
(רץ בחשבון המכינה עם `drive.readonly`, שיש לו writer על התיקייה): `type=fileget&sid=&fid=&token=`,
מאמת מול `student_docs` שהקובץ באמת בתיקיית התלמיד, ומחזיר base64. ה-Edge Function נופלת לשם רק על 403.
- סוד `GAS_URL` נוסף ל-Supabase secrets. אומת חי: "כץ הלל ויתור סודיות.pdf" (38KB) ירד מהאתר.
- ⚠️ **נטפרי חוסם גם את העלאת הקוד ל-`script.googleapis.com`** (418 "badwords" על גוף הבקשה בעברית).
  העדכון בוצע דרך Edge Function זמנית ששלחה את הקוד ב-base64 מהענן. `clasp push` מקומי לא יעבוד.
- **מחיקת קבצים ישנים עדיין לא אפשרית** — דורשת שדרוג ההיקף במניפסט מ-`drive.readonly` ל-`drive`,
  ואז אישור חד-פעמי של בעל הסקריפט (6787012). עד אז מוצגת הודעה "פתח בדרייב".

## יצוא והדפסה + תיקוני מייל (2026-08-20)
- **`js/exporter.js` — מסך "יצוא והדפסה"** (`exporter` ב-MODULES, מותר למנהל/מפקח/מחנך/מזכירה):
  מקור (תלמידים/נוכחות/מבחנים/מעקב קריאה/מה חסר בתיק) × כיתות × עמודות × מיון × כיוון × גודל.
  התצוגה המקדימה היא **`.ex-sheet` contenteditable בגודל A4** — עורכים ואז מדפיסים.
  הדפסה דרך `body.printing-sheet` + `@page` דינמי; `thead{display:table-header-group}` מחזיר את
  כותרת הטבלה בכל עמוד. גם יצוא CSV (עם הגנת CSV-injection).
- ⚠️ **`_APPS_SCRIPT_BRIDGE.gs` הוא המקור המלא של הגשר; `gas-bridge/Code.gs` היה עותק ישן**
  בלי `indexMailDrive`/`sbUpsertLinks`. דחיפה של העותק הישן **מוחקת את מנוע האינדוקס מהסקריפט החי**
  (קרה ב-20/08 ותוקן). לפני כל דחיפה — לוודא ש-`grep -c '^function'` נותן 25 ושהאינדוקס בפנים.
- **באג שיוך מיילים (תוקן):** `GmailApp.search` מחזיר **שרשורים**, והקוד לקח את ההודעה האחרונה —
  ולכן דיוור כללי להורים הופיע אצל **כל** התלמידים, עם התשובה של הורה אחר. זו גם דליפת פרטיות
  בין משפחות. עכשיו סורקים הודעה-הודעה: שומרים רק מה שההורה **שלח**, או הודעה שנשלחה אליו
  אישית (≤3 נמענים). אחרי הרצת אינדוקס: **383 מיילים** (היה 0) ו-674 קבצים.
- **שדות האם/האב מולאו מ-`reg`** (`נייד אם`/`שם האם`/`אימייל אם`/`נייד אב`/`שם האב`) ל-37 התלמידים —
  העמודות היו ריקות לגמרי, ולכן הכרטיס והיצוא הציגו עמודות ריקות.

## סריקה מקיפה 2026-08-20 (17 מסכים + לוגים + DB + דרייב)
**נקי:** כל 17 המסכים נטענים בלי שגיאות JS · אין דליפה אנונימית באף טבלה (19 נבדקו) ·
RLS מופעל עם policies בכל הטבלאות · אין רשומות יתומות · `profiles.active` default=false ·
אינדקס ייחודי על `attendance` קיים · הגשר החי מכיל את כל 25 הפונקציות.

**תוקן בסריקה:**
1. **דליפת שם→טלפון:** `email_by_name` היה מורשה ל-**PUBLIC** (לכן `revoke ... from anon` לבדו
   לא הספיק — חובה `revoke all ... from public`). anon קיבל את כתובת הכניסה של איש צוות לפי שמו,
   ועם סיסמת-ברירת-מחדל=טלפון זו השתלטות על חשבון. בוטל; כניסה-לפי-שם הוסרה מהלקוח.
2. **400 בכל טעינת "הגדרות":** `user_class_access` נטען עם `order=id` שלא קיים בטבלה.
   `noIdCol` ב-`api.js` מוזרע מראש עם `user_class_access`/`student_links`.
3. **הדפסה יצאה ריקה** — ראה סעיף היצוא.
4. **עמודות ריקות בכל המערכת:** תאריך לידה (לועזי+עברי), כתובת, שם/טלפון אב ואם היו ריקים
   ב-37 התלמידים; הערכים ישבו רק ב-`reg`. מולאו. ⚠️ בתאריכים היו **שני פורמטים מעורבים**
   (dd/mm וגם mm/dd, למשל 12/21/2013) — הזיהוי לפי איזה חלק > 12.

**פתוח ומודע:** סיסמת ברירת מחדל = מספר הטלפון (יוסף בחר להשאיר) · הרשמה פתוחה נדרשת
ליצירת צוות מהאתר, וממותנת בכך שפרופיל חדש נולד `active=false` · `readonly/writeonly`
נאכפים ב-CSS ולא ב-RLS · 6 תלמידים בלי תיקיית דרייב.

### עריכת טבלאות ביצוא (2026-08-20)
סרגל `#exEdit` מאפשר **הוספה/מחיקה של שורות ועמודות בכל מקור נתונים**, לא רק בטבלה הריקה.
המיקום נקבע לפי התא האחרון שנלחץ (`sel = {r,c}`, `r=-1` = שורת הכותרת). שורה חדשה יורשת
`style.height` מהשורה שמעליה — קריטי לטבלאות למילוי-יד. **יצוא ה-CSV נקרא מה-DOM** ולא
מ-`page._rows`, כדי שעריכות ידניות ייכנסו גם לקובץ. שינוי הגדרות בסרגל העליון בונה מחדש
ומאפס עריכות — יש על כך אזהרה במסך.

## ניהול צוות ותיקים אישיים (2026-08-20)
- **טבלת `staff`** (`supabase/migration_staff.sql`) — מספר עובד, ת"ז, כתובת, תאריך לידה,
  **פרטי בנק**, אילו מסמכים הוגשו, `user_id` אופציונלי ל-`profiles`, ו-`drive_id` לתיק האישי.
  ⚠️ **RLS = מנהל בלבד** (`staff_admin_all`) — הטבלה הרגישה ביותר במערכת. אומת: anon ומחנך רגיל
  מקבלים `[]`, ומחנך שמנסה `?staffId=` ב-Edge Function נחסם.
- **UI:** `js/staff.js` נטען לתוך `#staffCard` במסך ההגדרות. בטבלה מוצגים רק שם/מס׳/תפקיד/טלפון
  וחיווי מסמכים — ת"ז ומספר חשבון נפתחים רק בכרטיס.
- **ה-Edge Function `drive` מקבלת `staffId`** לצד `studentId`; ההרשאה נבדקת מול `staff`
  עם ה-JWT של המשתמש (RLS מחליט). `student-docs.js` הוכלל: `openManager(student, onChange, owner)`
  כאשר `owner={kind:'staff', id, name, folders}`.
- **דרייב:** תיקיית האם `תיקים אישים` = `1Dkf0_rMek_QNNK3-dc3r8FKumQTJ-VMv` (בבעלות 6742853).
  8 מתוך 13 שויכו לתיקיות קיימות (⚠️ **שתי מוסכמות שמות הפוכות** — "רפאל רוקמיל" מול "רוקמיל רפאל"),
  ול-5 נוצרו חדשות. בתיקייה יש גם תיקיות של עובדים לשעבר שאינם ברשימה.

### הפיכת איש צוות למשתמש (2026-08-20)
כפתור בשורה בטבלת הצוות → תפקיד + סיסמה ראשונית (ברירת מחדל = הטלפון) + כיתות → יוצר חשבון
דרך `signUp` ב-client זמני, משלים את הפרופיל (`active:true`) ומקשר ל-`staff.user_id`.
מספר שכבר רשום → מקושר לפרופיל הקיים במקום להיכשל.
⚠️ **`store.removeBy` לא היה קיים בפרויקט הזה** (רק בחיידר) — נוסף ל-`api.js`+`store.js`.
זה גם תיקן באג קיים ב-`admin.js`: עריכת כיתות הוסיפה שורות ל-`user_class_access` אך **לא מחקה
את הישנות** (`remove` לפי `id` בטבלה חסרת `id`), כלומר מורה שהוזז כיתה שמר את הגישה הקודמת.

## הורדת PDF ישירה (2026-08-20)
`js/pdf.js` + `vendor/html2canvas.min.js` + `vendor/jspdf.umd.min.js` (**מקומיים**, נטפרי חוסם CDN).
`cv3Pdf.save(el, name, {orientation, margin})` מצייר את האלמנט לקנבס, חותך לעמודי A4 ומוריד קובץ.
כפתור "הורד PDF" נוסף ב: **יצוא והדפסה** · **דשבורד ודוחות** · **כרטיס תלמיד** · **אישור לימודים** · **תל"א**.
- ⚠️ **`color-mix()` הפיל את html2canvas** ("unsupported color function") — כרום מחשב אותו ל-`color(srgb …)`,
  והעיצוב כאן בנוי עליו. הפתרון ב-`sanitizeColors` (hook `onclone`): מציירים כל צבע בעייתי על קנבס 1×1
  וקוראים את הפיקסל, כלומר הדפדפן ממיר ל-rgba. **אל תסיר את ה-hook** — בלעדיו הדוחות והכרטיס נופלים.
- בחלונות הקופצים (תל"א/אישור) הכפתור קורא ל-`window.opener.cv3Pdf` — הקובץ יורד דרך חלון המערכת.
- הטקסט ב-PDF הוא תמונה (לא לחיפוש). זו התמורה על שמירת העיצוב והעברית במדויק.

## תל"א — תכנית היעדים לפי הטופס הרשמי (2026-08-21, משוב נעמי לוי)
`supabase/migration_tla_goals_v2.sql`. המבנה (מימין לשמאל, כמו בטופס):
**יעדים** | **הזדמנויות עבודה ואמצעים להשגתם** (עמודה לכל איש צוות) | **מעקב והערכה** (מעצבת/מסכמת),
ומתחת: **המלצות** | **הערות ושינויים משמעותיים במהלך השנה**.
- `goals_list jsonb` = מערך יעדים (במקום `goal_sum`/`goal_exec` שלא היו בטופס; תוכנן הוגר).
- ⚠️ **`opps jsonb` = מערך `{who,what}` ולא אובייקט** — ב-jsonb סדר מפתחות של אובייקט אינו נשמר,
  ולכן סדר עמודות הצוות בהדפסה היה שרירותי. `roles` נשאר רק כנפילה אחורה.
- `eval_form` / `eval_sum` / `recommendations` — נוספו.
- **RPC `staff_names()`** (SECURITY DEFINER, `authenticated` בלבד) מחזיר שם+תפקיד לבחירה,
  כי טבלת `staff` נעולה למנהל. אל תרחיב אותו לשדות נוספים.
- כותרת "יעדים" ממוזגת ב-`rowspan=2` כדי להתאים לטופס.

### תיקיות "רישום תשפ"ז" (2026-08-21)
נעמי סידרה ב-`רישום תשפז` (`1LBmK2n5WN4PF2iuTiPEA7o0u0N_1nmQJ`, בבעלות zmw100) תיקייה לכל מועמד
עם תתי-תיקיות: מסמך קביל / שאלון הפניה / החלטת ועדה / צילום ת"ז / היסטוריה וחומרים קודמים.
**12 מהן שויכו לתלמידים** ב-`student_docs` (kind='תיקיית רישום תשפ"ז') — כולל 4 תלמידים שלא היה
להם שום מסמך. **לא שויכו:** סיגל, קנר, לוינסון, שינקר (אינם תלמידים במערכת) ו"ניזר יוסף"
(במערכת רשום **פנחס** ניזר — ממתין להחלטה). סה"כ 64 שורות drive ל-37 תלמידים.

## קו ימות המשיח של המכינה — נבנה מחדש (2026-08-24)
**הקו הוא `0733518751` ("תשע תלמוד"), לא 033060570** (זה קו החיידר; ה-spec הישן
`YEMOT_PANEL_SPEC.md` מיושן). סיסמת הקו ב-`.local/ym.py`. **יתרה = 0 יחידות** —
רק צינתוק חינמי (מי שנרשם בעצמו) עובד; קמפיין שמשמיע את ההודעה דורש טעינת יחידות.

**גיבוי מלא של המבנה הישן** (82 ext.ini + 127 קבצי שמע): `D:\yemot-backup\0733518751_2026-08-24`.
הקו היה קודם **תבנית "קבוצת שליחה" סטנדרטית של ימות** — נמחקה כולה חוץ משלוחה 9.

### המבנה
```
/        go_to_folder + go_to_folder_from_file=yes  → FileGoToFolder.ini מזהה את המתקשר
/0       תפריט ראשי (למי שלא זוהה) + שלוחות ניתוב 0/1..0/9 אל השלוחות האמיתיות
/1../4   שיעור א / ב / ג1 / ג2 — menu:  N/1 הודעות · N/2 רישום לצינתוק ·
         N/3 השארת הודעה (למייל) · N/9 עדכונים כלליים (folder_to_play=/6)
/5       השארת הודעה לחיצוניים      /6  עדכונים כלליים (יעד "לכולם")
/7       רישום לצינתוק 7/1..7/5      /8  הקלטות מנהל (password=2853)
/9       דיווח קולי לצוות — **אין לגעת**. white_list=yes + WhiteList.ini (12 מספרי צוות)
```
- **הנעילה בשלוחת השיעור אוטומטית:** `#`/`*` מחזירים לשורש, והשורש מזהה ומחזיר לאותה שלוחה.
- **`/8/N`** — הקלטה עם `folder_move=/N/1` ו-`list_tzintuk=N`; אחרי ההקלטה:
  1=אישור+צינתוק · 2=אישור בלי צינתוק · 3=מחדש · 4=המשך · 6=ביטול (`M1009.tts` בכל תיקייה).
  **"הקלט ואז בחר שלוחה" לא קיים בימות** — בחירת השיעור חייבת להיות לפני ההקלטה.

### סקריפטים
- `.local/build_line.py [clean]` — בונה את כל ה-ext.ini מאפס. מקור האמת למבנה.
- `.local/build_routing.py [push]` — מייצר את `FileGoToFolder.ini` מ-Supabase (92 מספרים).
  **מחריג אנשי צוות** לפי `ivr2:9/WhiteList.ini` — אחרת הם ננעלים בשלוחת השיעור
  ומאבדים גישה לשלוחה 9. אחים בשני שיעורים לא ממופים (מקבלים תפריט ראשי).

### פאנל הניהול בתוכנה
`js/yemot-line.js` — כרטיס "ניהול קו המכינה" בתוך מסך `yemot`: הודעות לשיעורים
(טקסט→קול Gemini, העלאה, מחיקה, צינתוק) · רשימות תפוצה · סנכרון זיהוי המתקשרים ·
עריכת טקסטי התפריטים. בדיקה חיה: `_qa_yemot_line.mjs` (13/13).

### ⚠️ מלכודות שנתפסו
- **`window.Yemot.getText` מחזיר מחרוזת או null — לא אובייקט תגובה.** `r.contents` = undefined
  והמסך מציג "ריק" בשקט.
- **`TYPE_META` ב-`yemot.js` כתב פרמטרים שלא קיימים בימות** (`record_max_seconds`,
  `record_no_review`, `play_default_file`, `transfer_phones`, `api_url`) — משם הגיע
  ה-ext.ini השבור של "ברכת המפיל". הוחלפו במאומתים. **אל תמציא פרמטרים** — ראה
  `~/.claude/projects/C--/memory/yemot-ext-ini-rules.md`.
- **`uploadBlob` דרס תמיד את `000.wav`** — נוסף `autoNumbering`.
- **עטיפה כפולה של `sys.stdout`** ב-`.local/*.py` מול `ym.py` סוגרת את הזרם
  (`I/O operation on closed file`). להשתמש ב-`PYTHONIOENCODING=utf-8`.

### צינתוק — מה עולה ומה לא (אומת חי מול ה-API, 2026-08-24)
| קריאה | תוצאה |
|---|---|
| `RunTzintuk` + `phones=0501234567,...` | **0.1 יחידה לשיחה**; ביתרה 0 מחזיר `Low unit balance` |
| `RunTzintuk` + `phones=tzl:<רשימה>` | `biling: "0.00"` — **חינם**, עובד ביתרה 0 |
| `TzintukimListManagement` | דורש `action`; **כל 22 השמות שנוסו נדחו** (`action is Unknown`) |
| `GetTemplates` / `GetTemplateEntries` | עובדים — תבנית הקמפיין של הקו |

**המסקנה:** אי אפשר להזריק מספרים לרשימה החינמית — זה התנאי של ימות לחינם. שידור לכלל
ההורים חייב לעבור דרך `phones=` מפורש מנתוני המערכת, בתשלום. הפאנל מציג את מספר
המספרים והעלות **לפני** הלחיצה, בודק יתרה, ודורש `confirm`.

⚠️ **`RunTzintuk` מחייג באמת.** תבנית הקמפיין של הקו (`templateId` 378637) הכילה מספר אחד —
`0556742853` (יוסף) — וקריאת בדיקה חייגה אליו. **לבדוק `GetTemplateEntries` לפני כל הפעלה.**

**ספירות שונות לניתוב ולשידור, וזה נכון:** שלושת מספרי משפחת פטינו (בן בשיעור ב ובן בג2)
**מוחרגים מהניתוב** (מספר אחד לא יכול להצביע לשתי שלוחות) אבל **נספרים בשני השיעורים לשידור**.
לכן ניתוב=92 ושידור=95 (27/33/19/19).
