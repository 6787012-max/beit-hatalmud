# אבטחה ומוכנות לפריסה — cheder-v3

## עמדת אבטחה (מתוקנת מהגרסה הישנה)
| נושא | הגרסה הישנה | cheder-v3 |
|---|---|---|
| מיקום נתונים | GitHub **ציבורי** / Sheets | Supabase **פרטי** |
| הגנה על הנתונים | client-side בלבד (עקיף) | **RLS בצד-שרת** (`supabase/policies.sql`) |
| אימות | admin/6742, סיסמת 1234, OTP צד-לקוח | Supabase Auth (OTP/Magic-Link, צד-שרת) |
| סודות בקוד | טוקנים/מפתחות גלויים | **אין** — רק anon key (בטוח, RLS מגן) |
| הרצת פקודות מחשב | חשוף (pcExecute) | **לא קיים** |

## צ'קליסט אבטחה (עבר ✅)
- [x] אין סוד/טוקן/מפתח ב-`js/**` — רק `SUPABASE_ANON_KEY` (מוגן ב-RLS).
- [x] RLS מופעל על כל הטבלאות, deny-by-default; מנהל=הכל, מורה=כיתה שלו, רפואי=מוגבל.
- [x] הרשאות בצד-לקוח (`filterByPermissions`) הן UX בלבד — האכיפה ב-RLS.
- [x] אין דלתות אחוריות (אין admin/6742, אין 1234).
- [x] אין endpoint להרצת פקודות/מייל.
- [x] `escHtml` בכל הזרקת נתונים ל-DOM.

## צ'קליסט מוכנות לפריסה
- [x] QA ויזואלי מלא: **30/30 מסכים נקיים** (15 מודולים × דסקטופ+מובייל) — קונסולה נקייה, אפס גלישה.
- [x] PWA + offline (`sw.js`).
- [x] RTL עברי מלא, מצב לילה, רספונסיבי.
- [ ] **ליוסף:** יצירת פרויקט Supabase + הרצת `schema.sql`+`policies.sql` + מילוי `config.js` (ראה `SETUP_INSTITUTION.md`).
- [ ] **ליוסף:** הפעלת GitHub Pages + הגדרת Email Auth ב-Supabase.
- [ ] **ליוסף:** הגדרת המשתמש הראשון כ-`מנהל` בטבלת `profiles`.

## הערה
כל עוד `config.js` ריק — האתר במצב **הדגמה** (מסכים בלי נתונים אמיתיים). מרגע שמחברים Supabase — הכל חי ומאובטח.


---

## סריקת אבטחה ותקינות — 2026-08-19

### קריטי — תוקן: כל נתוני התלמידים היו חשופים לכל אדם שנרשם
שרשרת ההתקפה שאומתה חיה מול ה-API:
1. `POST /auth/v1/signup` פתוח ומחזיר טוקן מאומת מיידית (autoconfirm פעיל).
   ההרשמה **חייבת** להישאר פתוחה — `admin.js` יוצר משתמשי צוות דרך `signUp` עם ה-anon key.
2. הטריגר `handle_new_user` יצר פרופיל בלי לציין `active`, וברירת המחדל של העמודה היתה `true`.
3. לכן `is_staff()` החזיר `true` לכל נרשם.
4. `students.stu_read = is_staff()` → **כל 37 התלמידים נחשפו**: שם, תעודת זהות,
   שמות וטלפוני הורים, וכל שדות `reg` (כתובת, קופת חולים, ת"ז הורים, ארץ לידה).
   בנוסף נחשפו `institution_settings` (הבלנק הרשמי והחותמת — חומר לזיוף אישורים)
   ו-`reading_categories`.

**התיקון** (`supabase/migration_lockdown.sql`, הורץ): פרופיל חדש נולד `active=false`,
והמנהל מפעיל אותו במסך ההגדרות. אומת: נרשם חדש מקבל **0 שורות** מכל טבלה.
הצוות הקיים לא הושפע. חשבונות הבדיקה נמחקו.

מה שנבדק ו**לא** היה פגיע: הסלמת תפקיד ל"מנהל" (נדחתה), צירוף עצמי לכיתה (403),
כתיבה לטבלת התלמידים, `sign.html` (מברח כראוי), סודות בקוד (אין).

### הודק אגב כך
- `categories` היתה גלויה לכל מאומת → עכשיו `is_staff()`.
- `audit_log` / `feedback` — כל מאומת יכול היה להזריק רשומות מזויפות → עכשיו `is_staff()`.

### נותר פתוח (דורש החלטה)
- **סיסמת ברירת מחדל = מספר הטלפון** (`admin.js`). מספרי הטלפון של הצוות ידועים במוסד.
- **אין CSP** בעמודים.


---

## סריקת אבטחה — סבב 2 (2026-08-19)

בדיקה חיה עם חשבונות בכל תפקיד (מפקח/מלמד/מחנך) + סקירת כל ה-RPC וכספים.

### תוקן: טוקן חתימה פר-תלמיד היה חלש
`forms.js` יצר את הטוקן של כל `form_response` ב-`Math.random().toString(36).slice(2,10)` —
8 תווים (~41 ביט, ניתן לחיזוי). `get_signing(token)` מחזיר לפי הטוקן את **תשובות ההורה
והחתימה**. שודרג ל-`crypto.getRandomValues` (96 ביט) כמו הקישור הכללי. הטופס הציבורי
(`gTok`) כבר היה חזק.

### ממצאים שדורשים החלטה (לא שונו — משנים התנהגות)

1. **`email_by_name` מדליף טלפוני צוות לאנונימי.** הכניסה תומכת בשם → הלקוח קורא
   `email_by_name` (anon) שמחזיר `{טלפון}@bht.co.il`. כלומר כל אחד ברשת, לפי שם איש
   צוות (מופיע באתר), מקבל את הטלפון שלו. **בשילוב עם סיסמת ברירת מחדל = טלפון = השתלטות
   על חשבון.** אומת חי: החזיר את הטלפון של שני המנהלים. הפתרון: לבטל כניסה-לפי-שם
   (הפרויקט ממילא מיועד לכניסה-לפי-טלפון) ולשלול anon מ-`email_by_name`. תלוי בהחלטת
   הסיסמאות.

2. **מצבי readonly/writeonly נאכפים בצד-לקוח בלבד (CSS).** ה-RLS משתמש ב-`is_staff()`/
   `can_see_student`, לא ב-`access_mode`. אומת חי: **מפקח ("צפייה בלבד") הצליח להזריק
   `behavior_events` ו-`attendance` דרך ה-API הישיר** (201). ההסתרה במסך קוסמטית. מפקח
   הוא צוות מהימן, לכן הדירוג בינוני — אבל אכיפת readonly אמיתית דורשת בדיקת access_mode
   ב-policies.

### נבדק ותקין
כתיבה לכספים (`tuition`) נדחתה למפקח/מלמד/מחנך (403 — רק מזכירה/מנהל). מודל יתרת הקופה
(`tuition+income-expenses`) תקין. ה-RPC הציבוריים (`get_public_form`/`submit_public_form`)
חושפים רק טפסים פתוחים לפי קוד אקראי. אין SQL injection (הכל פרמטרי/SECURITY DEFINER עם
search_path קבוע). דליפת ה-signup מסבב 1 נותרה סגורה.
