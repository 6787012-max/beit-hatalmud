
.local/
_CREDENTIALS.md
.pentest_uid
_APPS_SCRIPT_BRIDGE.gs
gas-bridge/
_video/
_demo/
shots/
.claude/

# קבצי עבודה מקומיים (שמות תלמידים/מיפוי דרייב) — לא לריפו הציבורי
_students.json
_drive_*.json
_qa_*
node_modules/
_shots/
*.pdf

# מיילים יוצאים — מכילים שמות תלמידים, לא לריפו
_send_*.py
package-lock.json

# סרטוני הדרכה וחומרי הביניים — כבדים, לא לריפו
video/_build/
video/shots/
video/out/

# מסך הלובי — קונפיג מקומי של המחשב בלובי
lobby/lobby_config.json
lobby/__pycache__/

# נתוני מייל שנשאבו לצורך הזמנת הספרים — שמות וכתובות של הורים
_books_mail/
_mail_index_*.json

# סקריפט ייבוא עם טוקן ניהול — לעולם לא לריפו
_psp_import.py

# צילומי מסך של QA — מכילים שמות תלמידים אמיתיים, לא לריפו הציבורי
_shot_*.png
_qa_*.xlsx
